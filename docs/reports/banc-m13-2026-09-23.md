# Banc M13 — Prophet contre une boucle nue, même modèle — 23 septembre 2026

La suite de tâches de M13-T1 n'avait jamais été jouée par un vrai modèle. Le banc
([ADR 0048](../adr/0048-le-banc-prophet-contre-une-boucle-nue.md)) la joue deux fois avec le
modèle de l'image, Qwen3 1.7B en Q8_0, servi par le llama-server épinglé comme l'image le lance
(sans projection, fenêtre de 4 096 tokens, une place) :

- **par Prophet** : pour chaque tâche, une chaîne capd, ledger et agentd neuve ; la mission est
  planifiée avec une portée limitée au dossier de la tâche, lancée, attendue, publiée par
  `task.apply`, et le vérificateur lit le répertoire personnel ;
- **par une boucle nue** : la même boucle native et les mêmes implémentations d'outils
  fichiers, appelées sans registre, sans capd, sans journal, condensation ni rappel ; l'espace de
  travail est recopié dans le répertoire personnel, et le même vérificateur tranche.

Environnement : coureur `ubuntu-latest` de la CI, quatre cœurs x86-64, sans carte graphique ;
le modèle échantillonne comme l'image le règle (température 0,7, top-p 0,8, top-k 20).

## Résultats

### Premier passage (`f4d13c6`) : sept tâches, une exécution chacune

| Tâche | Prophet | Boucle nue |
|---|---|---|
| compter-lignes | ✗ 29,5 s, 3 étapes — écrit 3 au lieu de 5 | ✓ 44,9 s, 6 tours |
| trouver-le-contrat | ✗ 8,9 s, 2 étapes — rien d'écrit | ✗ 8,6 s, 2 tours — rien d'écrit |
| total-des-ventes | ✗ 28,3 s, 5 étapes — écrit « Total montant: 330 » au lieu de 425 | ✗ 20,3 s, 4 tours — rien d'écrit |
| rapport-trimestriel | ✗ 9,7 s, 2 étapes — rien d'écrit | ✗ 11,3 s, 2 tours — rien d'écrit |
| ranger-par-annee | ✗ 8,3 s, 2 étapes — rien de rangé | ✗ 18,3 s, 1 tour — rien de rangé |
| extraire-et-resumer | ✗ 22,9 s, 2 étapes — rien d'écrit | ✓ 40,0 s, 6 tours |
| ne-pas-toucher-au-reste | ✗ 9,7 s, 2 étapes — rien d'écrit | ✗ 5,5 s, 1 tour — rien d'écrit |
| **Bilan** | **0/7**, médiane 9,7 s, p95 29,5 s, 2 421 tokens en moyenne | **2/7**, médiane 18,3 s, p95 44,9 s, 3 284 tokens |

### Deuxième passage (`6d01c38`) : livrable rappelé, quinze tâches, trois exécutions chacune

| Tâche | Prophet | Boucle nue |
|---|---|---|
| compter-lignes | 1/3 (6 et 3 au lieu de 5) | 1/3 |
| trouver-le-contrat | 0/3 — rappelé, écrit « aucun fichier ne mentionne… » | 0/3 — rien d'écrit |
| total-des-ventes | 0/3 — 375, 33, puis panne de `fs.search` | 0/3 — 33 450, 325 |
| rapport-trimestriel | 0/3 — rappelé, écrit un résumé à 0 € | 0/3 — rien d'écrit |
| ranger-par-annee | 0/3 — rappelé, crée `out` sans ranger | 0/3 |
| extraire-et-resumer | 0/3 — rappelé, écrit sans avoir lu | **3/3** (six tours, trois lectures) |
| ne-pas-toucher-au-reste | **3/3** — rappelé à chaque fois | 0/3 — répond « je n'ai pas accès » |
| compter-les-erreurs | 0/3 — `fs.search` sur le fichier : panne, arrêt | 0/3 — même erreur, dix à quatorze tours |
| le-plus-gros-achat | 0/3 — refus hors portée, arrêt | 0/3 |
| corriger-une-faute | 0/3 — « il n'y a pas de faute » | 0/3 |
| tableau-de-l-equipe | 0/3 — `fs.list` refusé, arrêt | 0/3 |
| changer-le-port | 0/3 — « je ne peux pas modifier un fichier » | 0/3 |
| fusionner-les-listes | 0/3 — `fs.list` refusé, arrêt | 0/3 — neuf `fs.list` de suite |
| extraire-les-adresses | 0/3 — `fs.search` sur le fichier : panne, arrêt | 0/3 |
| la-derniere-sauvegarde | 0/3 — `fs.list` refusé, arrêt | 0/3 |
| **Bilan** | **4/45**, 15 exécutions rappelées ; médiane 15,4 s, p95 27,5 s, 2 415 tokens | **4/45** ; médiane 12,5 s, p95 60,8 s, 3 880 tokens |

Chaque exécution relève désormais ses outils et la réponse finale du modèle
(`banc.log`, artefact `banc-m13` de la CI).

## Lecture

Le premier passage ne mesure pas les couches de Prophet : il mesure le modèle. Cinq des sept
échecs de Prophet, et cinq de ceux de la boucle nue, ont la même forme — la mission finit
proprement en une ou deux interrogations, sans avoir écrit le fichier que l'objectif nomme :
le modèle a lu, puis répondu par du texte. Les deux réussites de la boucle nue sont des
exécutions où il a continué jusqu'à l'écriture (six tours) ; à température 0,7 et une seule
exécution par tâche, ce qui sépare les deux colonnes est l'échantillonnage, pas le système.
Les deux autres échecs sont du calcul (3 lignes comptées au lieu de 5, un total faux).

Deux décisions en sortent :

- **Rappeler le livrable** ([ADR 0049](../adr/0049-le-livrable-rappele-au-modele.md)) : quand
  l'objectif nomme un chemin que la portée couvre et qui n'existe pas encore, et que le modèle
  conclut sans l'avoir produit, agentd le lui rappelle et le laisse continuer, deux fois au
  plus, chaque interrogation comptée comme une étape et journalisée (`task.reminded`). C'est une
  couche de Prophet : la boucle nue ne l'a pas, et le banc en mesure l'effet.
- **Mesurer mieux** : chaque exécution relève désormais les outils appelés et la réponse finale
  du modèle, et chaque tâche se rejoue trois fois en CI ; le bilan compte les réussites par
  tâche.

### Ce que le deuxième passage a appris

- **Le rappel fait écrire.** Les quinze exécutions rappelées ont toutes écrit le fichier demandé,
  et « ne-pas-toucher-au-reste » passe de 0 à 3 sur 3. Mais le rappel ne rend pas le contenu
  juste : quand le modèle n'a rien lu d'utile (une recherche sans lecture), il écrit ce qu'il
  sait — « aucun fichier », « 0 € ».
- **Le prix était ailleurs : 19 des 45 exécutions par Prophet s'arrêtaient sur un refus (12) ou
  une « panne » (7).** Le manifeste du banc n'accordait pas `fs.list` (les profils locaux du
  système l'accordent) ; `fs.search` sur un fichier rendait `Internal` ; un chemin hors de la
  portée arrêtait la mission sans dire où agir. La boucle nue recevait les mêmes erreurs et
  continuait. D'où l'[ADR 0050](../adr/0050-un-refus-revient-au-modele.md) : un refus de chemin
  revient au modèle en disant où la mission peut agir, la révocation et le troisième refus
  arrêtent toujours ; lire un répertoire ou lister un fichier rend `Invalid` avec l'outil qui
  convient ; `fs.search` fouille un fichier nommé comme racine ; `fs.read` compte les lignes ;
  le banc accorde `fs.list`.
- **Le reste est le modèle** : Qwen3 1.7B nie la faute à corriger, se dit incapable de modifier
  un fichier qu'il pourrait réécrire, et additionne mal. Ni Prophet ni la boucle nue n'y
  changent rien ; un modèle plus grand du catalogue (`PROPHET_BENCH_MODEL`) le mesurera.

### Troisième passage (`63586c6`) : refus rendus au modèle, rappel conditionnel

| Tâche | Prophet | Boucle nue |
|---|---|---|
| compter-lignes | 1/3 | 0/3 |
| compter-les-erreurs | **3/3** — `fs.search` sur le fichier, rappelé, écrit 3 | 0/3 — écrit 8 |
| extraire-les-adresses | **3/3** | **3/3** |
| tableau-de-l-equipe | 1/3 — lit puis écrit le tableau | 0/3 — écrit sans lire |
| extraire-et-resumer | 0/3 — écrit sans avoir lu | **3/3** |
| ne-pas-toucher-au-reste | 0/3 — rappelé, décline | 0/3 |
| les neuf autres | 0/27 | 0/27 |
| **Bilan** | **8/45**, 29 exécutions rappelées ; médiane 26,1 s, p95 50,8 s, 3 737 tokens | **6/45** ; médiane 24,1 s, p95 59,3 s, 4 494 tokens |

Ce que coûtent les couches de Prophet, mesuré pour la première fois : **0,27 s de processeur
par mission** pour capd, le journal et agentd réunis, et **47 Mo** de mémoire résidente au plus,
quand le moteur prend en moyenne 114 s de processeur par mission (117 s pour la boucle nue) —
0,2 % du calcul. L'énergie n'est pas lisible sur ce coureur (machine virtuelle, sans compteurs
RAPL) : le temps processeur en tient lieu.

Ce qu'il a appris :

- **Rendre le refus au modèle paie** : plus aucune mission ne s'arrête à la première erreur de
  chemin ; `compter-les-erreurs` passe de 0 à 3 sur 3, `extraire-les-adresses` de 0 à 3 sur 3
  des deux côtés (même implémentation de `fs.search`).
- **Un rappel conditionnel se décline** : 13 des 29 exécutions rappelées concluent sans écrire,
  et `ne-pas-toucher-au-reste` retombe de 3 à 0 sur 3. La consigne revient en tête du rappel,
  l'issue en fin (ADR 0049).
- **L'ordre des outils différait** : le registre de Prophet les offre par nom, la boucle nue
  dans l'ordre de leur déclaration, et les deux côtés n'ouvraient pas leurs missions par les
  mêmes outils. La boucle nue les offre désormais dans le même ordre, sous un essai.

### Quatrième passage (`390aed1`) : outils dans le même ordre, consigne en tête du rappel

| Tâche | Prophet | Boucle nue |
|---|---|---|
| compter-lignes | 1/3 | 0/3 |
| compter-les-erreurs | **3/3** | 0/3 |
| tableau-de-l-equipe | 1/3 | 2/3 |
| extraire-les-adresses | 2/3 | 3/3 |
| la-derniere-sauvegarde | 1/3 | 0/3 |
| extraire-et-resumer | 0/3 | 0/3 (3/3 au passage précédent) |
| les neuf autres | 0/27 | 0/27 |
| **Bilan** | **8/45**, 30 exécutions rappelées dont 25 écrivent ; médiane 22,9 s, p95 53,8 s, 4 088 tokens, 96 s de processeur moteur | **5/45** ; médiane 8,6 s, p95 35,5 s, 2 472 tokens, 48 s de processeur moteur |

- **L'ordre des outils comptait** : offerts dans le même ordre, la boucle nue perd les trois
  réussites d'« extraire-et-resumer » qu'elle devait à sa liste commençant par `fs.read`. L'écart
  précédent sur cette tâche ne disait rien des couches de Prophet.
- **La consigne en tête est suivie** : 25 des 30 exécutions rappelées écrivent (16 sur 29 avec
  le rappel conditionnel). « ne-pas-toucher-au-reste » décline encore : le modèle conclut sans
  outil, trois fois.
- **Le prix de Prophet est dans le modèle, pas dans les services** : ses missions réussissent
  plus souvent parce qu'elles continuent — rappel, refus rendu au modèle — et consomment donc
  deux fois plus de processeur moteur et 1,7 fois plus de tokens que la boucle nue, qui conclut
  plus tôt. Les services, eux, restent à 0,28 s de processeur et 47 Mo par mission.
- **Deux tâches restent hors de portée d'un outil qui réécrit tout** (« corriger-une-faute »,
  « changer-le-port », 0 sur 12 par côté en quatre passages) : `fs.edit` remplace un passage
  exact ([ADR 0051](../adr/0051-fs-edit-et-les-outils-d-exploration.md)), offert des deux côtés.

### Cinquième passage (`06c432d`) : `fs.edit`, nom cherché dans le contenu, second rappel net

| Tâche | Prophet | Boucle nue |
|---|---|---|
| corriger-une-faute | **3/3** (0 sur 12 auparavant) | **3/3** |
| trouver-le-contrat | **3/3** (0 auparavant) | 2/3 |
| extraire-les-adresses | 3/3 | 3/3 |
| ne-pas-toucher-au-reste | 2/3 | 0/3 |
| compter-lignes | 1/3 | 0/3 |
| tableau-de-l-equipe | 0/3 | 1/3 |
| compter-les-erreurs | 0/3 (3/3 au passage précédent) | 0/3 |
| les huit autres | 0/24 | 0/24 |
| **Bilan** | **12/45**, 25 exécutions rappelées dont 24 écrivent ; médiane 29,8 s, **p95 170 s**, 11 588 tokens, 208 s de processeur moteur | **9/45** ; médiane 15,0 s, p95 57,7 s, 5 755 tokens, 99 s |

- **`fs.edit` débloque la correction** : « corriger-une-faute » passe de 0 à 3 sur 3 des deux
  côtés ; la recherche d'un nom dans le contenu débloque « trouver-le-contrat » ; le second
  rappel, plus net, remonte « ne-pas-toucher-au-reste » à 2 sur 3.
- **Mais `fs.edit` ouvre un piège** : le modèle essaie d'éditer le fichier qu'il doit créer et
  recommence à l'identique, jusqu'à vingt-quatre fois (« tableau-de-l-equipe » : 380 s, 68 353
  tokens) ; « compter-les-erreurs » retombe à 0. D'où le p95 de 170 s. L'[ADR 0052](../adr/0052-les-echecs-repetes.md)
  le désamorce : `fs.edit` sur un fichier absent renvoie vers `fs.write`, une note suit le
  deuxième échec identique, et cinq échecs de suite arrêtent la mission.
- Les services de Prophet : 0,55 s de processeur et 47 Mo par mission (des missions plus
  longues).

## Reproduction

```sh
nix build .#llama-cpp --out-link moteur
PROPHET_TEST_LLAMA_SERVER=$PWD/moteur/bin/llama-server just bench
```
