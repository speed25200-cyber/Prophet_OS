# `shell` — vues destinées à l'humain

Des fonctions pures d'un état vers du texte : elles se testent sans lancer quoi que ce soit et
servent aussi bien au binaire `prophet` qu'à la surface graphique. Plan d'une tâche, timeline,
centre d'approbations, résumé de statut.

```sh
cargo test -p shell
```
